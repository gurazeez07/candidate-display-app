import React from 'react';

const FilterBox = ({
  deptProgram,
  setDeptProgram,
  gender,
  setGender,
  raceEthnicity,
  setRaceEthnicity,
  credentials,
  setCredentials,
  status,
  setStatus,
  handlePageChange,
  currentPage,
  totalPages,
}) => {
  return (
    <>
      {/* Dropdown menus */}
      {/* ... (All the dropdowns and pagination controls code) */}
    </>
  );
};

export default FilterBox;
